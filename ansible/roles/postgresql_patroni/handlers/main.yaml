---
# handlers file for docker
- name: reboot
  systemd:
    daemon_reload: true

- name: start 
  systemd:
    name: postgresql.service
    state: started
    enabled: true

- name: restart
  systemd:
    name: postgresql.service
    state: restarted